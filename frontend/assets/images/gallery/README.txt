Drop your own photos here named photo-1.jpg through photo-6.jpg (or more —
see the main README) and they'll appear in the Gallery carousel
automatically, shown in full (no cropping) with a soft blurred backdrop
filling any empty edges. Missing files show a small placeholder instead of
a broken image, so it's fine to leave slots empty for now.
